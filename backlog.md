# Backlog

Ordered by severity: [C] critical/crash, [H] potentially bad, [M] can wait, [L] cosmetic.

## [C] Critical / crash

(none open)

## [H] Potentially bad

(none open)

## [M] Can wait

Ordered by user-facing value if fixed (2026-08-25) - active pain points and near-shipped fixes first, pure internal/design/infra notes last, two already-mostly-shipped status notes at the very bottom by request.

- [2026-08-16] [M] No warning before a cache-invalidating action. Prompt caching is a prefix match, so several cockpit controls silently throw away the session's warm cache and the user only finds out afterwards, via the stats strip's hit rate dropping. Three tiers, worst first: (a) tool-set changes - MCP toggle/reconnect and reload-plugins - tools render at prefix position 0, so any add/remove/reorder invalidates tools + system + messages; (b) model switch - caches are model-scoped, so the whole prefix is gone; (c) thinking budget/display - invalidates the messages cache only, tools + system survive. usage.js's accumulator already tracks `cacheReadTokens`, so the cost of the impending re-write is computable at click time: warn (inline, `.mcp-error` pattern) when the session has more than some threshold of cached tokens, naming which of the three tiers applies. Scope/approach not designed: where the threshold sits, whether it's a confirm dialog or a passive note, and whether the warning belongs on the control or in the stats strip. The cockpit cannot set `cache_control` itself - prompt assembly belongs to the Agent SDK's `query()` - so avoiding needless invalidation is the only lever; do not let the copy imply we could extend the TTL. Separately, do not attribute plain idle-gap expiry (the default cache TTL is 5 minutes, refreshed on every read) to a user action - a warning that cries wolf after a coffee break is worse than none.

- [2026-08-18] [M] MCP "needs-auth" badge now has an "Authenticate" link (mcp-panel.js) once one actually appears - design decision made: the SDK exposes exactly one mechanism for this, `onElicitation` (mode 'url', a `url` to open in-browser; completion arrives later as a separate `elicitation_complete` system message, not synchronously). Wired end to end: session.js's onElicitation stores the pending {url, message} per server name and fire-and-forget-accepts (form-mode elicitation is declined outright - no UI for arbitrary JSON-schema forms, out of scope); session-registry.js's getMcpServerStatus merges it into the SDK's own status list as authUrl/authMessage and pushes a live `cockpit:mcp-auth` broadcast so a settings modal left open through the flow doesn't sit on a stale badge. Unit-covered at both layers (session.test.mjs, session-registry.test.mjs). **Not hand-verified live** - this sandbox has no real OAuth-requiring MCP server. If it doesn't work against a real needs-auth server (wrong field names, mode assumptions, elicitation firing before vs. after 'needs-auth' status, etc.), that's the first thing to check.

- [2026-08-17, closed 2026-09-02] [M] CLOSED as unreproducible. Cost-graph viewport slider (turn-chart.js's #turnChartSlider) reported not sliding in sync with the transcript in the live app - "cost graph doesn't slide when the horizontal bar slides (only the main display)." The code read looks correct on paper - sliderTrack's mousedown, the window mousemove drag handler, and scrollContainer's own 'scroll' listener all call updateSlider() (turn-chart.js:92-166), and the module comment even documents a prior version of this exact bug as fixed by making that call synchronous. Three independent attempts to reproduce live (2026-08-17 investigation, 2026-08-18 - Bertrand directly, and a 2026-09-02 architectural review pass) all failed to reproduce or find a code path that could cause it. Reopen with exact repro steps and transcript size/state if it resurfaces.

- [2026-08-20, prereqs shipped 2026-08-25, blocker found 2026-09-02] [M] MVP7 cannot be a `@media` tweak. There is one breakpoint (`max-width: 900px`, stacks the detail pane). Loopback is unreachable from a phone; even LAN Origin/Host would 403 (`http://192.168.x.x:4317` is not in the allowlist). v1 should be a dedicated `/approve` page (pending request, Allow/Deny/always-allow, AskUserQuestion) on the same SSH/Tailscale tunnel as MVP6, pairing via QR of `{origin, operatorToken}`, operator token in `Authorization` not the URL. Full mobile session view is later. Both prerequisites are done: the operator token exists (`operator-auth.js`), and pending approvals are a queue (`row.pendingApprovals[]`, `approvalQueue` in app.js) instead of a single last-write-wins slot. `public/approval-panel.js` is already a standalone importable module (dependency-injected DOM refs + `postDecision`/`sendFollowUpInput` callbacks), so rendering isn't the blocker. The real blocker, found while scoping this 2026-09-02: `GET /api/sessions` (operator-token-gated, no session token) returns `toSummary()` rows, which deliberately omit each row's own bearer token and full pending-request detail (only `session-registry.js`'s `getDebugInfo`/`memorySnapshot` see `pendingApprovals` at all, and neither is a public route) - there is currently no way for an operator-token-only caller to see what's pending across every session or decide one, by design (session tokens are the per-session decision boundary today). Needs an explicit design call before building: either (a) a new operator-token-gated route that surfaces `pendingApprovals` detail and accepts decisions without handing out the session's own bearer token, or (b) skip the cross-session view for v1 and have `/approve` require pasting in one session's own id+token, same access a browser tab already has. Not done yet either way.

- [2026-08-20, closed 2026-09-02] [M] CLOSED. No Content-Security-Policy or X-Frame-Options on static/HTML responses. Shipped: `applySecurityHeaders` in `server.js` now sets CSP/X-Frame-Options/nosniff/Referrer-Policy/COOP/CORP on every response. `markdown.js` already built via textContent not innerHTML - kept that.

- [2026-08-20, restated 2026-08-24, body cap + WS payload cap closed 2026-09-02] [M] Network-exposure leftovers, ship with the operator-token work not after. Closed: `readJsonBody` (http-utils.js) now rejects a body over 1MB with a 413; the WebSocket transport now sets `maxPayload` at that same 1MB limit (`server.js`), closing the socket with code 1009 on an oversized frame instead of buffering it unbounded - fixing this exposed a separate latent bug (no `ws.on('error', ...)` listener, which Node throws as an uncaught exception with nothing attached) that got fixed alongside it. `isSessionTrusted` compares handshake secrets with `timingSafeEqual` instead of `===` (shipped earlier, session-registry.js). Still open: the websocket upgrade puts the session token *and* the operator token in the query string (`/ws?id&token=&op=`), which leaks to logs and referrers - move both to a first message or an Authorization-equivalent before a tunnel exists (2026-09-02 review, "Security ranked" list - the one item on that list still not done).

- [2026-08-20, gate shipped 2026-08-25] [M] MVP6 recommended shape (no design doc yet): two cockpit processes, each still bound to 127.0.0.1, talking over an SSH tunnel. A "remote session" on the Mac is a proxy row whose `pushInput` is an HTTP/WS call to the Windows process, not a local `query()`. Do not listen on `0.0.0.0` and do not add the peer's Origin to the allowlist. Handshake is the delegation group secret, not the wire credential - the operator-token gate now covers `/api/handshake` (and every other `/api/*` and `/ws` route), so it is no longer returned to a browser that isn't already operator-authenticated; still needs the handshake secret itself to stop being reused as the wire credential once a real remote session type exists. `~/.claude/plans/claude-prompt-cockpit.md` does not exist; designing MVP6 starts from scratch.

- [2026-08-20, sizes 2026-08-24, all gravity wells addressed 2026-08-26] [M] CLOSED. `session-registry.js` (~83 KB) was the live hub, the delegation protocol, usage/context/rate-limits, queue mirroring, handshake, and auto-continue. MVP6 wants a second row type (not locally stamped, not a local CLI handle) - handshake trust and `/ask` (`delegateTask`/`relayDelegationResult`/`collectDelegationText`/`failPendingDelegations`/the handshake secret itself) are now split into `src/delegation.js` (`createDelegation()`, dependency-injected with `sessions`/`pushTurn`/`broadcast`/`broadcastSummary`/`findByName` so there's no import cycle back into session-registry.js). session-registry.js's own public exports (`getHandshakeSecret`, `delegateTask`, etc.) are unchanged re-exports, so no caller had to change. `pendingResultTags` itself stayed in session-registry.js/on the row at the time - it's shared FIFO turn-tracking used by every turn, not delegation-specific, so it wasn't part of the split. (Superseded later: the row array is gone entirely, and `src/result-epoch.js` now owns both turn order and the per-turn delegation tag - see its module comment.) session-registry.js: ~1808 -> ~1446 lines; delegation.js: ~378 lines new. Client-side approval UI was already addressed separately: `public/approval-panel.js` (`initApprovalPanel()`, split alongside `session-controls-panel.js` in `a917bd2`) is its own importable module - a future MVP7 `/approve` page can import it instead of copy-pasting from app.js. The three remaining gravity wells are now also done: `public/stream-view.js` (~50 KB) had its cross-session-delegation message rendering (the `[Prompt Cockpit] Relayed ...` header parsing, the delegated-reply bubble tracking, the `cockpit:delegate-sent`/`cockpit:delegate-full-trace` marker handling) split into `public/delegate-view.js` (`createDelegateView()`, same dependency-injection shape as `delegation.js` - takes `appendBlock`/`closeGroup` as params so it never imports stream-view.js back), ~971 -> ~892 lines, verified live via Playwright against a real transcript with delegated messages (header correctly unwrapped, label/body/class all correct). `public/style.css` (~73 KB, no section structure to begin with) was sliced in original top-to-bottom order (cascade-preserving - a later file's rule still needs to win the same equal-specificity ties it did before) into 8 topic files (`style-base/settings/launcher/approval/transcript/panels/compose/overlays.css`), loaded via ordered `<link>` tags in index.html; verified byte-identical concatenation and live via Playwright screenshot. Caught and fixed a real pre-existing bug in the process: the `prefers-reduced-motion` override for `.task-status-in_progress` sat in a shared media-query block earlier in the file than the animation rule it was meant to defeat, so the equal-specificity source-order tie was silently won by the wrong rule - reduced-motion never actually disabled the pulse animation; moved the override to right after the rule it overrides. `tests/session-registry.test.mjs` (~85 KB) had its 24 delegation/handshake tests moved to `tests/delegation.test.mjs` (mirroring the source split), with the shared `fakeWs`/`fakeStartSession` test doubles extracted to `tests/test-helpers.mjs` so neither file duplicates them; ~1879 -> ~1139 lines, full suite still 474/474 throughout.

- [2026-08-15] [M] Code review improvement: zero test coverage on turn-chart.js/mcp-panel.js/plugin-panel.js - client-side, no jsdom harness in this project (see tests/README.md's established "hand-verified only" convention for public/*.js), so this needs that harness built first, not just more node --test cases. The server-route half of this item (model/thinking/auto-continue/mcp/plugin-enabled/reload-plugins routes, including 401s) is done - see server-auth.test.mjs. Manual suites (`tests/integration.manual.mjs`, `tests/grok-*.manual.mjs`) are still the only live-SDK coverage. Codex has no equivalent live manual test.

- [2026-08-15] [M] data-turn-point index coupling between turn-chart.js and stream-view.js is comment-enforced ordering only across three files - fragile, but replacing it needs a real design call (explicit turn-uuid attributes instead of positional ordering? something else?), not a mechanical fix. The "Query method + row patch + broadcast" duplication half of this item is fixed - see session-registry.js's queryPassthrough().

- [2026-08-20, header + anchor shipped 2026-08-25] [M] MVP6 seed, shipped: a per-process delegation handshake secret (`session-registry.js`'s `handshakeSecret` / `getHandshakeSecret` / `regenerateHandshakeSecret` / `setSessionHandshake` / `isSessionTrusted`), minted fresh at every server start, in memory only. Every locally-created session row is stamped with the current value at creation (trusted by construction); `delegateTask` refuses unless BOTH origin and target currently match the server's canonical value. Surfaced in two places: the cross-tab "All sessions" pane (session-list-pane.js) shows the server's own copyable value plus a rotate button (global, no session token - same Origin/Host-only gate as `/api/browse`), and each session's own Settings panel shows ITS OWN trust status plus a paste-in field to re-sync (scoped to that session's own bearer token, via `POST /api/sessions/:id/handshake`). Pairing is deliberately manual copy/paste only - no exchange protocol, no persistence, no actual cross-machine transport. This is infrastructure for MVP6, not MVP6 itself: there is still no remote/SSH session type, no pairing UI beyond copy-paste. `buildDelegatedHeader` now cites the handshake check explicitly, and Claude sessions get a `systemPrompt` append anchoring the `/ask` protocol as legitimate before any relay ever arrives (session.js; no Grok-side equivalent mechanism exists). Rotating the secret is an intentionally blunt "cut everyone off" control - it does not re-stamp existing rows, so it also doubles as an audit ("who's still trusted after I rotate is who actually re-synced").

- [2026-08-18, (d) shipped 2026-08-25] [M] MVP5 cross-session delegation (`/ask <Name>: <text>`) v1 gaps, flagged as known limits at ship time (see `~/.claude/plans/nifty-mixing-bee.md` for the full design): (a) same-cwd-only routing - closed via `findTrustedByName`; (b) no delegation-loop guard beyond a single self-delegation check - A asks B asks A is not prevented, could spiral conversationally; (c) relay is text-only (final assistant answer), no tool-call trace forwarded - confirmed v1 scope, not an oversight, but worth revisiting if a delegated task's *how* turns out to matter as much as its *what*; (d) `/ask` name autocomplete shipped 2026-08-25 (`ask-picker.js`) - dropdown opens above compose with other named same-cwd sessions (not the caller), a single sibling auto-completes `/ask Name: ` (colon required); (e) still purely user-triggered - an LLM has no way to delegate on its own initiative (no real SDK tool_use path), by design for v1.

- [2026-08-25, root cause found and tokens fixed 2026-09-02] [M] Reported: per-turn usage (tokens, and presumably cost derived from them) doesn't show for Codex sessions - stats-panel.js/turn-chart.js read it fine for Claude and Grok. Actual root cause (a 2026-09-02 architectural review): not codex-session.js dropping the field - `codex-messages.js`'s `usageFromTokenUpdate()` already builds a correct token breakdown and stamps it onto the assistant message. The real bug was one layer up, in `usage.js`'s `costForUsage`/`createUsageAccumulator` and its call site in `session-registry.js`'s `applyAssistantUsage`: an unpriced model (no pricing.json/pricing_grok.json entry, which is every Codex model - there is no pricing_codex.json) short-circuited the whole message, dropping its real token counts along with the unknowable cost, so `message._usageInfo` never got set at all and neither the stats strip nor the cost graph got anything to read. Fixed: `costForUsage` now returns the real token breakdown with `cost: null` for an unpriced model instead of returning `null` outright (which it now only does when there's no usage to report at all) - tokens show for Codex (and any future unpriced model) immediately; `_usageInfo.costUsd` reads 0 and the model is flagged in `usage.unpriced`, same "understated, not silently wrong" treatment `stats-panel.js` already had UI for. Cost still won't show for Codex until it has a real rates table - that's `pricing_codex.json`, closed 2026-09-02 (see the Global Stats note above).

- [2026-09-02] [M] `sessionDefaults.effort` (session-defaults.js, per-cwd persistence) is not scoped per-provider. Last writer wins across providers sharing a cwd - a Grok session set to `xhigh` can seed a Claude session started next in the same folder with an effort value Claude may not even support, silently (2026-09-02 architectural review, "Provider parity" table).

- [2026-09-02] [M] `GET /api/history/:id` (and its provider-agnostic siblings) fall back to `process.cwd()` (the cockpit *server's* own working directory) when no `cwd` query param is given, rather than requiring one - a caller that forgets the param reads whatever project the server process happens to be running from, not an error (2026-09-02 review, "Security ranked" list).

- [2026-09-02] [M] `bypassPermissions` sits one Shift+Tab tap past `plan` in the permission-mode cycle (src/permissions.js) - the most dangerous mode is not the hardest to reach by accident. Move it last in cycle order (2026-09-02 review, "Security ranked" list).

- [2026-09-02] [M] Git-commit-guard (Settings > Project tab) is a Claude-only PreToolUse hook (only Claude sessions actually enforce it), but the Settings UI shows the same toggle regardless of which provider the session is using, with nothing indicating Grok/Codex sessions aren't covered - same "silently Claude-only, unlabeled" shape as the Global Stats item above (2026-09-02 review, "Security ranked" list).

- [2026-09-02] [M] Frontend cleanup, `public/app.js` (2026-09-02 architectural review), in order: (1) drive remaining UI off `session.capabilities` - kill the leftover `currentProvider === 'grok'|'claude'` string checks in the model badge and MCP notes, same migration the launch-model tables already got (see the "Grok backend" app.js entry below); (2) do not silently drop a send when `ws` is not OPEN - queue locally or toast "reconnecting", and guard `JSON.parse` in `ws.onmessage` (the server already swallows bad JSON, the client does not); (3) the file-picker needs a generation counter or `AbortController` for its async suggestions fetch, same pattern the resume list's `resumableGen` already uses; (4) Shift+Tab steals reverse-tab-through-the-page focus whenever any session is live, not just while compose has focus - restrict it or pick a different chord; (5) `--detail-pane-offset` is a singleton CSS variable - opening History's own detail pane can shove the live session's compose box over.

- [2026-09-02] [M] Claude rewind (`src/rewind.js`'s `resolveTurnUuid`/`forkConversation`/`rewindFiles`) is not unit-testable the way Grok's own rewind already is - it calls the SDK's `getSessionMessages`/`forkSession`/`query.rewindFiles` directly rather than through an injectable seam. Needed before any confident change to rewind logic (2026-09-02 review, "Test gaps" list).

- [2026-09-02] [M] Grok has no `/ask` system-prompt append. Claude sessions get `ASK_SYSTEM_PROMPT_APPEND` (session.js) anchoring the cross-session delegation protocol as legitimate before any relay ever arrives; grok-session.js has no equivalent. Unconfirmed whether Grok's ACP even exposes a comparable system-prompt-append mechanism - check that first, then use it if it exists (2026-09-02 architectural review).

## [L] Cosmetic

- [2026-08-20, restated 2026-08-24, fixed 2026-09-02] [L] CLOSED. Docs/metadata drift: CLAUDE.md described two providers and server.js as the whole HTTP surface; now mentions Codex, `src/provider-registry.js`'s descriptor boundary, and the `src/routes/*.js` split. tests/README.md's opening file list named ~10 files and drifted every time a test file was added since; now points at the directory instead of hand-listing it, so it can't drift the same way again. package.json's description now mentions all three providers. grok-session.js's and codex-app-server.js's ACP/app-server `clientInfo.version` now match package.json's version (both were manually kept in sync this once - still not automated, so this can drift again on the next version bump; worth a build-time check if that starts to matter).

- [2026-08-24, closed 2026-09-02] [L] CLOSED. Global Stats is Claude-only (`src/global-stats.js` walks `~/.claude/projects/**/*.jsonl`); Grok and Codex spend never appeared there, unlabeled. Badged rather than hidden or filtered (the two options this item itself proposed): the Settings tab is now titled "Stats (Claude)" and its own note leads with "Claude Code sessions only - Grok and Codex spend isn't tracked here" (`index.html`). Claude-centric persistence (titles, defaults, plugins, permission rules, git-commit-guard in the target project's `.claude/settings.local.json`) is a known tradeoff, not a bug. Pricing: `pricing_codex.json` now exists (rates cross-checked against OpenAI's own per-model docs pages, 2026-09-02) alongside the Claude and Grok tables - Codex cost now shows instead of $0/unpriced for the three models it covers (gpt-5.1-codex, gpt-5.1-codex-mini, gpt-5.3-codex); any other Codex model still falls through to the unpriced path. Claude rates last verified ~2026-08-06 to 2026-08-09; Grok ~2026-08-16.

- [2026-08-20] [L] Delegated-reply relay (`buildDelegatedHeader`'s `'result'` branch) should include the target session's turn cost alongside the answer, so the origin session sees what the delegated ask cost, not just its text. More useful once the target is a remote box you cannot see.

- [2026-08-20] [L] `tag.buffer` (session-registry.js, backing a delegated turn's relay) is unbounded - a long-running delegated turn accumulates every text block in memory with no cap or truncation before shipping it all into the origin's context as one turn.

- [2026-08-24] [L] `GROK_BIN` / `CODEX_BIN` are taken as given. A local attacker who can set env is already on the machine. Sharp edge, not a network hole.

- [2026-08-14] [L] In plan mode's end-of-plan display, add the ability to open the plan .md file in the default markdown opener (overridable in settings), and add the ability to append more to the plan before approving (e.g. "spawn opus high-effort agent to validate the plan").

- [2026-08-15] [L] Add a "/btw" slash command - idea only, scope/behavior not yet specified. Its prerequisite (a visible queue/steer UI, so an aside doesn't just look like another queued message) shipped 2026-08-17 - see queue-panel.js/session.js's listQueue/removeQueued/reorderQueue/sendNow.

- [2026-08-15] [L] In the message stream's viewable area, highlight the cost value (.usage-meta) of the single most costly call (by cost/tokens in/tokens out/cached - metric TBD, probably cost) in the same color as turn-chart.js's cost bar (`#f0b90b`), so the priciest turn stands out without opening the cost graph. Scope/approach not yet designed (recompute on every stream update? only within the current viewport? which of the four metrics wins if they disagree on which turn is "most costly"?).

- [2026-08-17] [L] Not started - image paste/attachments (Grok CLI parity review). Deferred deliberately (see plan doc) - drop-target.js is path-only on purpose since a browser tab can't see a dropped file's real path. query() does accept image content blocks per the SDK, but this needs a real server-side upload or file-read path, not another compose-side hack. Scoped as its own mini-project, not a quick win. From a review comparing Grok CLI's session controls against this cockpit (2026-08-17, see conversation for the full comparison table) - shipped items from that review aren't listed here; check git log on `feature/session-qol-controls` for what already landed (cancel-the-running-turn was first).

- [2026-08-18] [L] Full project plan/roadmap doc: `~/.claude/plans/claude-prompt-cockpit.md` does not exist on disk (checked while planning MVP5 - either deleted or never written despite being referenced here). MVP1-5 shipped (see README.md); MVP6 (Windows-hosted session over SSH) and MVP7 (phone access/approvals) have no design doc at all yet - designing MVP6 needs to start from scratch, not from a nonexistent prior doc.

- [2026-08-24] [L] Codex rewind only if the app-server grows a fork. Do not fake Claude rewind. Codex also has no MCP/plugins/thinking/auto-continue/project always-allow - the descriptor is honest (`unsupported()` stubs).

- [2026-08-27] [L] Comment-tone/length pass across the codebase - trim comments that read as AI-generated or lean on irrelevant references, and treat any comment over 4 lines as suspicious by default. Scale check done: 430 comment blocks over 4 lines across 74 files (src/ + public/*.js), worst offenders app.js (64 blocks, up to 24 lines), session-registry.js (59 blocks, up to 36 lines), session.js (36 blocks), stream-view.js (32 blocks). Real risk flagged before starting: many of these long comments document hard-won gotchas (SDK quirks, race conditions, CSS-specificity bugs) that cost a real debugging session to find - a blind length-based trim could delete load-bearing "why," not just fluff. Proposed approach not yet started: calibrate on one small file first (detail-pane.js suggested), confirm the trim bar against a real before/after diff, then apply file by file, asking when a comment's necessity is genuinely ambiguous rather than guessing.

- [2026-09-02] [L] Test gaps flagged by the 2026-09-02 architectural review, none tied to a known live bug: no traversal test (`..`/encoded `..%2F..%2F..`) against `serveStatic`'s own path confinement; no `codex-*.manual.mjs` sibling to `grok-live.manual.mjs`/`integration.manual.mjs` - Codex's `/ask` target is the least-exercised live delegation path of the three providers.

- [2026-09-02] [L] Grok's ACP `clientInfo.name` (grok-session.js's `CLIENT_INFO`) is still `'claude-prompt-cockpit'` from before Codex existed - harmless (Grok does not act on it differently), just wrong-looking in a protocol trace (2026-09-02 architectural review).

- [2026-09-02] [L] `CLAUDE.md` still describes two auth layers and token-free `/api/browse`/history routes - reality is operator-token-gated on every `/api/*` route and the WS upgrade (`operator-auth.js`), a layer added after that description was written (2026-09-02 architectural review, docs drift).

- [2026-09-02] [L] CLOSED. Cleanup batch from a 2026-09-02 architectural review (refactor/dead-code/simplification findings; the "proposed new features" half of that review - pricing_codex.json, Global Stats provider scoping, security headers, the `/approve` page, a debug-snapshot ring buffer - was deliberately left undone, per the maintainer's own scoping, and stays open above/here as its own items). Fixed in this pass: dead imports/re-exports removed from `session-registry.js` (stale `startSession`/`startGrokSession`/rewind.js/grok-rewind.js/session-history.js/grok-history.js imports, the unused `GROK_EFFORTS`/`CLAUDE_EFFORTS` re-export), unused exports un-exported or deleted in `codex-app-server.js` and both `operator-auth.js` files; `session-registry.js`'s `claudeSessionId` dual-write removed (now derived in `toSummary()` from `providerSessionId` + `row.provider === 'claude'`, so a Grok/Codex row no longer claims a `claudeSessionId` that's really its own id); `provider-registry.js`'s `resolveEfforts` now fails closed (returns `null`, `session-actions.js` answers 503) on a live model-catalog fetch failure instead of silently falling back to the full static effort superset; `escapeHtml` deduped into `public/escape-html.js` (was defined twice, one DOM-trick version and one regex version); `src/routes/session-actions.js` refactored from a 520-line 24-branch if-chain with the same try/catch copy-pasted each time into a ~447-line `ACTIONS` lookup table with one shared dispatcher (`RouteError` carries custom status codes through it; every existing 400/404/409/503 preserved - one minor, net-positive side effect: three GET routes that previously had no local try/catch now get the same `{error}` JSON 500 shape as everywhere else on an unexpected throw, instead of the router's plain-text 500 fallback); the LCS diff engine extracted from `stream-view.js` (1076 -> 1022 lines) into `public/diff-lines.js` with its own `tests/diff-lines.test.mjs` (previously untested); `public/app.js`'s hardcoded `CLAUDE_START_MODELS`/`GROK_START_MODELS`/`CLAUDE_EFFORT_OPTIONS`/`GROK_EFFORT_OPTIONS`/`*_EFFORT_LABELS` tables and the `launchKind()`/ternary-ladder provider-string branching deleted, moved into `provider-registry.js`'s descriptors as `models`/`effortOptions` and served over the existing `/api/providers` payload the launcher already fetched pre-launch (`app.js` now branches on `capabilities.thinkingBudget` + presence of `launch.effortOptions` instead of `provider === 'grok'`/`'claude'` string checks) - live CLI model enumeration was explicitly out of scope, `GROK_START_MODELS`' contents are still a hand-maintained static list, just single-sourced now.

- [2026-09-02] [M] CLOSED. Unified the dual-FIFO turn tracking flagged by the same 2026-09-02 review - `row.pendingResultTags` (session-registry.js) and each handle's `createResultEpochTracker()` (result-epoch.js) used to be two independently-mutated copies of the same in-flight-turn ordering, hand-mirrored at nine call sites, with `reorderPendingTagsTail`/`reorderTail` being line-for-line duplicates. `result-epoch.js` is now the single owner, split into ORDER (`pending`/`abandoned`, unchanged mechanics) and DELEGATION TAGS (a new `tags: Map<queueId, tag>`, looked up by identity, never by position) - see that file's own header comment for the three separate production bugs this specifically closes off (a late result from a force-idled turn stealing a new turn's tag; a queue-panel drag moving the in-flight turn's tag to the back; sendNow unshifting ahead of it), plus one latent bug it fixes along the way (a Grok `runPrompt` error used to strand its delegation tag forever - tags are now released explicitly via `claimTag`/`takeAllTags`, never tied to `consume()`). `session-registry.js`'s `pendingResultTags` field, `reorderPendingTagsTail()`, `dropResultTag()`, and the three-way staleness cross-check in the `result` branch are all gone. Touched `result-epoch.js`, `session-registry.js`, `session.js`, `grok-session.js`, `codex-session.js`, `delegation.js`; 9 tests migrated (not dropped) via new `test-helpers.mjs` helpers, +6 new tests in `result-epoch.test.mjs`, full suite 487/486/1 (was 481/480/1), plus `node tests/integration.manual.mjs` run live against a real Claude session post-merge (both checks passed - init arrival and queue-while-running, the exact path this touched). Two things flagged as needing a human glance rather than fully verified: Codex's `result` message carries its `_cockpitQueueId` from `currentMeta()` rather than the `consume()` stamp (timing quirk of when Codex's notification subscriber vs. `runTurn`'s microtask fire) - reasoned through but not exercised live, so a delegation *to* a Codex session is the least-covered case here; and `pendingTurnsCount` for Codex may lag by one microtask on a finishing turn (believed invisible, not confirmed against the spinner badge).

## Grok backend (still out of scope)

- File-content rewind (Grok does not offer it; do not fake Claude rewind)
- Cross-provider resume
- Live MCP reconnect on a running Grok ACP session (toggle persists; start a new session to pick it up)
- [2026-08-26, recount 2026-09-02] app.js is now 2593 lines (was 2699 - the provider launch-table migration below deleted six hardcoded model/effort tables and a couple of ternary ladders, offset by a small new capability-driven helper), still above the thermo-nuclear-code-quality-review skill's 2000-line bar. Next candidates: the ~150-line block of loose getElementById DOM-lookup consts near the top; the recent-folders block (loadRecentFolders/rememberRecentFolder/renderRecentFolders); the agents-list rendering block (renderAgentsList/applyAgentArmedIndicator); and other per-feature settings-wiring blocks that could move into their owning modules' own init(), same pattern as settings-security-panel.js. Frontend has no jsdom test coverage (see tests/README.md), so any further split needs a manual browser click-through of the affected UI, not just a diff audit, before merging.

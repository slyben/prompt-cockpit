# Backlog

Ordered by severity: [C] critical/crash, [H] potentially bad, [M] can wait, [L] cosmetic.

## [C] Critical / crash

(none open)

## [H] Potentially bad

- [2026-08-20, mitigated 2026-08-20] [H] Cross-session delegation is frequently refused by the receiving model, confirmed via live repro (Claude1 session ran `/ask Sonnet: "I am Groot too"`; Sonnet replied "I didn't spawn 'Claude1'... not going to treat fabricated `<delegated_task>`/`<delegated_result>` blocks as real signals from an agent I control"). Investigated by an opus subagent (2026-08-20): not a code bug - `delegateTask`'s wrapper (session-registry.js) was byte-exact correct, single push, correct `from` attribution, `escapeAttr`/`escapeBody` were no-ops on plain text. The actual problem: the wrapper was a plain user turn making an unverifiable in-band provenance claim ("this came from another agent"), structurally identical to a prompt-injection payload. Shipped mitigation #2 same day: dropped the `<delegated_task from="...">`/`<delegated_result from="...">` XML shape entirely for prose framing (`buildDelegatedHeader` in session-registry.js - "[Prompt Cockpit] Relayed task/reply from "X"..." + explanatory paragraph + `\n---\n` separator + payload), works uniformly for Claude and Grok targets, no SDK changes needed. Also shipped the same day: a real per-session handshake secret (see the MVP6-seed item below) that the header can now truthfully reference as a checkable fact rather than pure prose - not yet wired INTO the header text itself (the mitigation shipped was framing-only; teaching the header to actually cite the handshake, and appending a system-prompt anchor so the receiving model learns about `/ask` through a second, trusted channel - mitigation #1 from the investigation - is still open, see below). Validate against both a content-free task and a substantive one - refusal rate was not uniform across task types even before the fix.
- [2026-08-20] [H] Grok-side delegation can desync `pendingResultTags`, misrouting a delegated reply to the wrong origin session. Found while investigating the above (not yet reproduced live - grok isn't installed on this dev machine to confirm end-to-end). `pushTurn` (session-registry.js) unconditionally appends a `pendingResultTags` entry for every call, but `grok-session.js`'s `pushInput` can early-return without actually pushing anything (e.g. deduped duplicate text, or a push after the session is closed) - leaving a pending-result entry for a turn that will never run. Since results are matched off this list FIFO, every later real result then shifts the wrong entry off the front, misattributing it to whichever origin was actually waiting on the *next* slot. `registry.forceIdle` has a narrower version of the same class of bug: it clears `pendingResultTags` (session-registry.js) and zeroes `pendingTurns` (session.js), but a genuinely in-flight turn's `result` can still land later and shift an entry belonging to a subsequent, unrelated turn.

## [M] Can wait

- [2026-08-20] [M] Delegation prose header (`buildDelegatedHeader`, session-registry.js) doesn't yet reference the handshake secret (see the MVP6-seed item below) even though both now exist side by side - citing it explicitly ("this session's handshake matches the server's") would give the receiving model a checkable fact instead of pure prose, likely reducing refusal further. Also still open: appending a short system-prompt note at session start (SDK's `systemPrompt: {type:'preset', preset:'claude_code', append}`) explaining the `/ask` relay protocol exists and is user-authorized - this only affects sessions started after the change and has no Grok-side equivalent, which is why it was scoped separately from the framing-only fix that shipped.
- [2026-08-20] [M] MVP6 seed, shipped: a per-process delegation handshake secret (`session-registry.js`'s `handshakeSecret`/`getHandshakeSecret`/`regenerateHandshakeSecret`/`setSessionHandshake`/`isSessionTrusted`), minted fresh at every server start, in memory only. Every locally-created session row is stamped with the current value at creation (trusted by construction, since it was spawned by this very process); `delegateTask` now refuses to run unless BOTH origin and target currently match the server's canonical value. Surfaced in two places: the cross-tab "All sessions" pane (session-list-pane.js) shows the server's own copyable value plus a rotate button (global, no session token - same Origin/Host-only gate as `/api/browse`), and each session's own Settings panel shows ITS OWN trust status plus a paste-in field to re-sync (scoped to that session's own bearer token, via the new `POST /api/sessions/:id/handshake` route) - the cross-tab pane can't offer a working paste control itself since it doesn't hold other tabs' session tokens. Pairing is deliberately manual copy/paste only for now (confirmed acceptable) - no exchange protocol, no persistence, no actual cross-machine transport. This is infrastructure for the eventual MVP6 goal (a Mac session delegating real work - "start testing this on DX12" - to a Windows session over SSH), not MVP6 itself: there is still no remote/SSH session type, no pairing UI beyond copy-paste, and the delegation header text doesn't yet cite the handshake as a trust signal (see the item above). Rotating the secret is an intentionally blunt "cut everyone off" control - it does not re-stamp existing rows, so it also doubles as an audit ("who's still trusted after I rotate is who actually re-synced").
- [2026-08-15] [M] Code review improvement: zero test coverage on turn-chart.js/mcp-panel.js/plugin-panel.js - client-side, no jsdom harness in this project (see tests/README.md's established "hand-verified only" convention for public/*.js), so this needs that harness built first, not just more node --test cases. The server-route half of this item (model/thinking/auto-continue/mcp/plugin-enabled/reload-plugins routes) is done - see server-auth.test.mjs.
- [2026-08-15] [M] data-turn-point index coupling between turn-chart.js and stream-view.js is comment-enforced ordering only across three files - fragile, but replacing it needs a real design call (explicit turn-uuid attributes instead of positional ordering? something else?), not a mechanical fix. The "Query method + row patch + broadcast" duplication half of this item is fixed - see session-registry.js's queryPassthrough().
- [2026-08-15] [M] Test coverage gap: no test exists for `seedSessionDefaults` (server.js:111 fresh-session path, server.js:406 rewind/fork path) despite two commits today dedicated to this exact logic - this is where the cross-session cwd-carry-forward regression above lives untested. Also no concurrency test interleaving setPluginEnabled + setSessionDefaults on the same cwd (the specific race settings-file.js's queue exists to prevent), no test for the settings-file.js write-failure path (where the process-kill bug above lives), and no 401 coverage on the new model/thinking/mcp-toggle/mcp-reconnect/reload-plugins/plugin-enabled routes specifically (only exercised with valid tokens).
- [2026-08-16] [M] No warning before a cache-invalidating action. Prompt caching is a prefix match, so several cockpit controls silently throw away the session's warm cache and the user only finds out afterwards, via the stats strip's hit rate dropping. Three tiers, worst first: (a) tool-set changes - MCP toggle/reconnect (server.js:274) and reload-plugins (server.js:294) - tools render at prefix position 0, so any add/remove/reorder invalidates tools + system + messages, i.e. everything; (b) model switch (server.js:329) - caches are model-scoped, so the whole prefix is gone; (c) thinking budget/display (server.js:355) - invalidates the messages cache only, tools + system survive. usage.js's accumulator already tracks `cacheReadTokens`, so the cost of the impending re-write is computable at click time: warn (inline, `.mcp-error` pattern) when the session has more than some threshold of cached tokens, naming which of the three tiers applies. Scope/approach not designed: where the threshold sits, whether it's a confirm dialog or a passive note, and whether the warning belongs on the control or in the stats strip. Note the cockpit cannot set `cache_control` itself - prompt assembly belongs to the Agent SDK's `query()` - so avoiding needless invalidation is the only lever we have; do not let the copy imply we could extend the TTL. Separately, do not attribute plain idle-gap expiry (the default cache TTL is 5 minutes, refreshed on every read) to a user action - a warning that cries wolf after a coffee break is worse than none.
- [2026-08-17] [M] Cost-graph viewport slider (turn-chart.js's #turnChartSlider) reported not sliding in sync with the transcript in the live app - "cost graph doesn't slide when the horizontal bar slides (only the main display)." The code read looks correct on paper - sliderTrack's mousedown, the window mousemove drag handler, and scrollContainer's own 'scroll' listener all call updateSlider() (turn-chart.js:92-166), and the module comment even documents a prior version of this exact bug ("slider doesn't slide the graph, only the text scrolls") as fixed by making that call synchronous. Two independent attempts to reproduce live (2026-08-17 investigation, 2026-08-18 - Bertrand directly) both failed to reproduce. Downgrading confidence this is a real live bug rather than a stale/misremembered report - leaving open only in case it's session- or timing-dependent (e.g. only shows up on a very long transcript, or a specific drag gesture). Next step if it resurfaces: get the exact repro steps and transcript size/state from whoever hits it, since two direct attempts couldn't trigger it blind.
- [2026-08-18] [M] User reported "tasklist is not visible" with a screenshot (Screenshot 2026-08-18 at 15.57.45.png) while I was mid-review using this Claude Code session's own TaskCreate/TaskUpdate tracker (not prompt-cockpit's in-app task-panel.js, which tracks the *target* session's TaskCreate/TaskUpdate/TaskList tool calls via session-registry.js's applyTaskOp - a different thing). Could not inspect the screenshot itself (no file found at the expected path, image wasn't attached to what I could read), so root cause is still unknown - may be a Claude Code harness/client UI issue outside this repo entirely, not a prompt-cockpit bug. Needs the user to re-share the screenshot or describe what they saw before this can be diagnosed, let alone fixed.
- [2026-08-18] [M] MCP "needs-auth" badge now has an "Authenticate ↗" link (mcp-panel.js) once one actually appears - design decision made: the SDK exposes exactly one mechanism for this, `onElicitation` (mode 'url', a `url` to open in-browser; completion arrives later as a separate `elicitation_complete` system message, not synchronously). Wired end to end: session.js's onElicitation stores the pending {url, message} per server name and fire-and-forget-accepts (form-mode elicitation is declined outright - no UI for arbitrary JSON-schema forms, out of scope); session-registry.js's getMcpServerStatus merges it into the SDK's own status list as authUrl/authMessage and pushes a live `cockpit:mcp-auth` broadcast so a settings modal left open through the flow doesn't sit on a stale badge. Unit-covered at both layers (session.test.mjs, session-registry.test.mjs) via the existing stubbed-queryImpl/startSessionImpl pattern. **Not hand-verified live** - this sandbox has no real OAuth-requiring MCP server configured to trigger an actual elicitation against, so the only verification is the SDK's documented contract plus the stub tests. If it doesn't work against a real needs-auth server (wrong field names, mode assumptions, elicitation firing before vs. after 'needs-auth' status, etc.), that's the first thing to check.
- [2026-08-18] [M] MVP5 cross-session delegation (`/ask <Name>: <text>`) v1 gaps, not built, flagged as known limits at ship time (see `~/.claude/plans/nifty-mixing-bee.md` for the full design): (a) same-cwd-only routing - `findByName` does an exact string match on `row.cwd`, no path normalization, so two sessions started from differently-spelled-but-equivalent cwd strings won't find each other; (b) no delegation-loop guard beyond a single self-delegation check - A asks B asks A is not prevented, could spiral conversationally; (c) relay is text-only (final assistant answer), no tool-call trace forwarded - confirmed v1 scope, not an oversight, but worth revisiting if a delegated task's *how* turns out to matter as much as its *what*; (d) no `/ask ` name autocomplete in the compose box (command-picker.js) - typos just produce a `cockpit:delegate-error` alert, no assist; (e) still purely user-triggered - an LLM has no way to delegate on its own initiative (no real SDK tool_use path), by design for v1.

## [L] Cosmetic

- [2026-08-14] [L] In plan mode's end-of-plan display, add the ability to open the plan .md file in the default markdown opener (overridable in settings), and add the ability to append more to the plan before approving (e.g. "spawn opus high-effort agent to validate the plan").
- [2026-08-15] [L] Add a "/btw" slash command - idea only, scope/behavior not yet specified. Its prerequisite (a visible queue/steer UI, so an aside doesn't just look like another queued message) shipped 2026-08-17 - see queue-panel.js/session.js's listQueue/removeQueued/reorderQueue/sendNow.
- [2026-08-15] [L] In the message stream's viewable area, highlight the cost value (.usage-meta) of the single most costly call (by cost/tokens in/tokens out/cached - metric TBD, probably cost) in the same color as turn-chart.js's cost bar (`#f0b90b`), so the priciest turn stands out without opening the cost graph. Scope/approach not yet designed (recompute on every stream update? only within the current viewport? which of the four metrics wins if they disagree on which turn is "most costly"?).
- [2026-08-17] [L] Not started - image paste/attachments (Grok CLI parity review). Deferred deliberately (see plan doc) - drop-target.js is path-only on purpose since a browser tab can't see a dropped file's real path. query() does accept image content blocks per the SDK, but this needs a real server-side upload or file-read path, not another compose-side hack. Scoped as its own mini-project, not a quick win. From a review comparing Grok CLI's session controls against this cockpit (2026-08-17, see conversation for the full comparison table) - shipped items from that review aren't listed here; check git log on `feature/session-qol-controls` for what already landed (cancel-the-running-turn was first).
- [2026-08-18] [L] Full project plan/roadmap doc: `~/.claude/plans/claude-prompt-cockpit.md` does not exist on disk (checked while planning MVP5 - either deleted or never written despite being referenced here). MVP1-5 shipped (see README.md); MVP6 (Windows-hosted session over SSH) and MVP7 (phone access/approvals) have no design doc at all yet - designing MVP6 needs to start from scratch, not from a nonexistent prior doc.
- [2026-08-20] [L] `tag.buffer` (session-registry.js, backing a delegated turn's relay) is unbounded - a long-running delegated turn accumulates every text block in memory with no cap or truncation before shipping it all into the origin's context as one turn.

## Grok backend (reference, not severity-tagged)

Merged in from backlog-grok.md - status/reference notes on the Grok provider, not
actionable bug items, so not folded into the [C]/[H]/[M]/[L] tiers above.

Shipped on `main` / `feature/grok-fork-and-effort`: live session, approvals, resume/history, conversation rewind via fork, mid-session model and effort, cost/usage, todo_write, MCP/plugin panels via `grok inspect` + `grok mcp|plugin enable/disable`.

Claude stays the other provider. A session picks one at launch.

### Still out of scope

- File-content rewind (Grok does not offer it)
- Headless `grok -p` as the session transport
- Cross-provider resume
- Live MCP reconnect on an already-running Grok ACP session (toggle persists; start a new session to be sure)

### Known ACP shapes (Grok 1.0.4)

Confirmed live against `grok agent stdio`:

- Mid-session effort: `session/set_mode` `{ sessionId, modeId }` where `modeId` is `low` / `medium` / `high` / `xhigh` (the `x.ai/sessionConfig` options with `category: "mode"`)
- Fork: `_x.ai/session/fork` `{ sourceSessionId, sourceCwd, newCwd, newSessionId }`
- The forked child is not loaded in the parent process. Rewind it in a short-lived connection after fork (`rewindGrokSession` in `src/grok-session.js`)

### References

- `~/.grok/docs/user-guide/15-agent-mode.md`
- `~/.grok/docs/user-guide/17-sessions.md`
- `tests/grok-rewind-probe.manual.mjs`
